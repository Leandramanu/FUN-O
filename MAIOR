#include<stdio.h>

int MaiorN(int x, int y)
{
    if(x > y)
    {
        printf("%d", x);
    }
    else
    {
        printf("%d", y);
    }
}
int main(){

    int x, y;
    scanf("%d", &x);
    scanf("%d", &y);

    MaiorN(x,y);

    return 0;
}
